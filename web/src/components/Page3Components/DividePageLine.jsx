function DividePageLine() {
  const styles = {
    border: `0`,
    height: `1px`,
    width: `100%`,
    backgroundColor: `#ffffff5f`,
  };

  return <div className="container" style={styles} />;
}

export default DividePageLine;
